@model IEnumerable<WebApplication1.Models.Ispit>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.predmet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Datum)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.predmet.PredmetID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Datum)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IspitId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IspitId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IspitId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
